import { CharacterLimitDirective } from './character-limit.directive';

describe('CharacterLimitDirective', () => {
  it('should create an instance', () => {
    const directive = new CharacterLimitDirective();
    expect(directive).toBeTruthy();
  });
});
